export const metadata = {
  title: "Order Confirmed | Al Sadeq Spices",
  robots: { index: false, follow: false },
};

export default function OrderConfirmationLayout({ children }) {
  return children;
}
